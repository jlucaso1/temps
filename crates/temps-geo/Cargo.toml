[package]
name = "temps-geo"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true

[dependencies]
anyhow = "1.0"
axum = { workspace = true }
chrono = { version = "0.4", features = ["serde"] }
log = "0.4"
maxminddb = "0.30.0"
# Already in the dependency graph via minidump (Sentry relay); enabling it here
# adds no new crate.
memmap2 = "0.9"
tempfile = { workspace = true }
moka = { version = "0.12", features = ["future"] }
rand = { workspace = true }
sea-orm = { version = "1.0", features = ["sqlx-postgres", "runtime-tokio-rustls", "macros"] }
serde = { version = "1.0", features = ["derive"] }
thiserror = { workspace = true }
temps-core = { path = "../temps-core" }
temps-database = { path = "../temps-database" }
temps-entities = { path = "../temps-entities" }
temps-auth = { path = "../temps-auth" }
utoipa = "5.1"
tracing = { workspace = true }
tokio = { version = "1.0", features = ["time"] }

[dev-dependencies]
tokio = { version = "1.0", features = ["full"] }
